# Phase 3: フロントエンド

## 目標

地図によるエリア選択と日付フィルタを備えた、見やすい大会スケジュール画面を実装する。

## 背景・動機

既存サイトのリスト形式では大会を探しにくい。
地図UIと日時フィルタで「行ける大会」を直感的に見つけられる体験を提供する。

## アプローチ

- 生HTML + Vanilla JS（フレームワーク不使用）で実装
- `Leaflet.js` + 都道府県GeoJSONで首都圏地図を表示
- 都道府県クリックでエリア絞り込み → APIからデータ取得 → カードリスト更新

## タスク

- [ ] `src/frontend/` ディレクトリ作成・ファイル構成決定（index.html, style.css, app.js）
- [ ] Leaflet.js導入・首都圏（東京・神奈川・埼玉・千葉）のGeoJSON読み込み
- [ ] 都道府県ポリゴンのクリックイベント実装（選択/解除のトグル）
- [ ] 日付レンジピッカー実装（`<input type="date">` × 2）
- [ ] APIからデータ取得（`fetch`）→ カードリスト描画
- [ ] フィルタ変更時のリスト再描画
- [ ] ローディング表示・エラー表示の実装
- [ ] レスポンシブ対応（モバイルで地図とリストが使えること）
- [ ] ローカルでの動作確認（地図クリック・日付変更でリストが正しく絞り込まれること）

## 意思決定ログ

- Vue.js は状態管理の複雑さがなく不要と判断。生JS + Leaflet.js で十分（詳細: `docs/design-docs/tech-stack.md`）
- 日付ピッカーは外部ライブラリ不使用・`<input type="date">` で対応

## 完了条件

- 首都圏地図が表示され、都道府県クリックでエリア絞り込みができること
- 日付レンジを変更すると大会リストが更新されること
- モバイルでも操作できること
